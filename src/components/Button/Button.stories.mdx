import { Meta, ArgsTable, Canvas, Story} from '@storybook/addon-docs';
import { Button } from './'

<Meta 
    title="Components/Button" 
    parameters={{
        backgaround: '#3d518c'
    }}    
/>

# Button

## A simple button

<Button text={"Button"}/>

<Canvas>
  <Story
    name="Button component"
    args={{
      text: 'Button',
      background: '#4350cc',
      color: '#25a18e',
    }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

## Disabled button

<Button text={"Disabled button"} disable/>

<Canvas>
  <Story
    name=" Disable button component"
    args={{
      text: 'Disabled button',
      disable: true,
    }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

<ArgsTable of={Button}/>
